{
  "name": "@krishisetu/architecture-tests",
  "version": "0.1.0",
  "private": true,
  "description": "Architectural boundary and invariant tests for KrishiSetu",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "node --test src/**/*.test.ts"
  },
  "type": "module"
}
