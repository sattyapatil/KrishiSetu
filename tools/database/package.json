{
  "name": "@krishisetu/database-tools",
  "version": "0.1.0",
  "private": true,
  "description": "Safe migration, seed, and prototype reset commands",
  "type": "module",
  "dependencies": {
    "@krishisetu/api": "*",
    "@krishisetu/config": "*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit"
  }
}
